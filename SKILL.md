---
name: laoke-kb-architect
slug: laoke-kb-architect
displayName: 知识库搭建助手
version: 1.0.0
author: 局内人·老K
description: 通用知识库搭建助手，把长篇素材按标题层级切成结构化知识条目，自动生成分类标签摘要与去重指纹，配套信息架构分块策略与质量检查清单
compatibility: 通用（支持 Skill 的 Agent：Claude Code / CodeBuddy / DeepSeek / WorkBuddy 等）
permissions: 读取本地 Markdown/文本素材、运行内置 Python 切片脚本、输出带 frontmatter 的条目文件与 manifest 索引
safety: 仅用于合法合规的知识整理与结构化；内置护栏硬拒侵犯隐私、侵权复制、虚假造假、违禁内容等违规请求
---

# 知识库搭建助手

> 这个 Skill 是给「东西越攒越多、越找越慢、脑子越来越乱」的人配的知识库架构师。它把一份长篇素材（文章、笔记、访谈稿、产品文档、研报、FAQ）按标题层级和语义边界，切成一条条能直接入库、能检索、能喂 AI 的结构化知识条目，并自动给每条配好分类、标签、摘要和去重指纹。不止「切一刀」，更配套一套信息架构、分块粒度、条目模板和入库质检的方法论，让你建对、建干净、建完真能用。
> 适用对象：做内容运营的知识工作者、带团队要沉淀 SOP 的负责人、要把资料接进 ima 知识库 / 向量检索 / Agent 的人、学生和研究者的第二大脑、客服/产品/技术写文档的团队。

---

## 一、这个 Skill 能干什么（能力边界）

**能做：**
1. 长文切片：把一份 Markdown / 纯文本，按 H2/H3 标题层级和段落边界，切成多条独立知识条目（见 references/03、scripts/kb_chunk.py）。
2. 自动元数据：每条自动推导 `category`（分类）、`tags`（标签）、`summary`（摘要）、`source`（来源）、`fingerprint`（去重指纹）。
3. 结构化输出：每条写成带 YAML frontmatter 的 `.md` 文件，外加一份 `manifest.json` 全量索引，直接可进 ima 知识库、向量库、笔记软件。
4. 方法论配套：信息架构怎么分（references/02）、切块切多大（references/03）、条目长什么样（references/04）、入库前怎么质检（references/05）、什么时候该建库（references/01）。
5. 增量更新依据：`fingerprint` 内容哈希可做去重与增量比对，避免重复建库。
6. 合规护栏：内置 `hooks/guardrail.md`，硬拒侵犯隐私、侵权复制、虚假造假、违禁内容等请求。

**不能做（护栏）：**
- 不能联网抓取、不能爬他人私有数据、不能突破付费墙/登录限制。
- 不能伪造出处、不能把未证实内容写成「权威定论」。
- 不能删除你任何文件（脚本只读输入、只写输出目录）。
- 不替代人工判断：切片后仍需你过一遍质检清单（references/05）。

---

## 二、核心工作流（五步走）

```
素材（长文 md）──▶ ① 判断是否该建库 ──▶ ② 定分类与受控标签
        │                                        │
        ▼                                        ▼
   ③ 跑 kb_chunk.py 切片 ──▶ ④ 过质检清单 ──▶ ⑤ 入库 / 喂 AI
```

1. **判断是否该建库**：满足「反复被问 / 多人协作 / 知识随人走 / 体量大 / 要喂 AI」任意两条就动手（详见 references/01）。
2. **定分类与受控标签**：先用 references/02 的 MECE 法把分类树画出来，列出 ≤30 个规范标签词。
3. **切片**：用 `kb_chunk.py` 把素材切成条目，注入你的受控标签（`--tags`）。
4. **质检**：按 references/05 清单逐条过，删碎片、切巨无霸、补标签。
5. **入库**：把 `.md` 条目 + `manifest.json` 导进 ima 知识库 / 向量库 / 笔记，或直接当 Agent 上下文。

---

## 三、详细使用步骤

### 场景 A：把一篇产品帮助长文变成可检索知识库

**第 1 步：准备素材**
把长文存成 `帮助中心.md`，确保用了 Markdown 标题（`#` 一级、`##` 二级、`###` 三级）。标题层级越清晰，切得越准。

**第 2 步：跑切片脚本**
```bash
python scripts/kb_chunk.py \
  --input 帮助中心.md \
  --out ./entries \
  --title "产品帮助中心" \
  --tags "产品,帮助中心,FAQ" \
  --max-chars 1800
```
- `--input`：素材路径（必填）。
- `--out`：输出目录（必填，会自动建）。
- `--title`：写入每条 `source` 字段，便于追溯。
- `--tags`：注入受控词表，逗号分隔，脚本不会乱造标签。
- `--max-chars`：单条最大字符数，默认 1800（覆盖人读 + 向量双目标）。

**第 3 步：看产物**
`./entries/` 下会多出：
- `账号与登录.md`、`会员与计费.md`、`数据安全.md` …… 每条带 frontmatter。
- `manifest.json`：全量索引，含每条的 slug/title/tags/summary/fingerprint。

**第 4 步：质检**
打开 `manifest.json` 抽样看 summary 是否含关键动作/数值，打开几条 `.md` 看是否独立可读。按 references/05 清单勾选。

**第 5 步：入库**
- 进 ima 知识库：把 `.md` 条目直接上传，frontmatter 的 tags 当标签、summary 当简介。
- 进向量库：用每条 `body` 做 embedding，检索返回后展示 `title + summary`。
- 当 Agent 上下文：把相关条目的 `body` 拼进 prompt。

### 场景 B：把一堆零散笔记整理成第二大脑

**第 1 步**：把笔记合并成一个大 `笔记合集.md`（用 `---` 或标题分隔各篇也行，脚本按标题切）。
**第 2 步**：跑切片，受控标签用「主题维度」：`--tags "想法,项目,资源,归档"`（对应 PARA 法，见 references/01）。
**第 3 步**：切片后人工把 `category` 归到 PARA 四类之一，标签做横向串联。
**第 4 步**：用 `fingerprint` 定期增量更新，新笔记只切新增部分。

### 场景 C：把研报 / 长文喂给 Agent 做问答

**第 1 步**：研报存 `研报.md`，标题层级写清楚（摘要/行业/竞争/财务/风险）。
**第 2 步**：`--max-chars 1200` 切得更碎，适配上下文窗口。
**第 3 步**：把相关条目 `body` 拼进 Agent prompt，让 Agent 基于「带出处」的条目回答，避免幻觉。
**第 4 步**：回答时让 Agent 引用 `source` 字段，可追溯。

### 场景 D：批量处理一个目录的多篇文档

`kb_chunk.py` 当前处理单文件。多文件时循环调用：
```bash
for f in docs/*.md; do
  python scripts/kb_chunk.py --input "$f" --out "./entries/$(basename $f .md)" --title "$(basename $f)" --tags "文档中心"
done
```
各子目录独立 manifest，最后可手动合并。

---

## 四、切片逻辑说明（透明可预期）

脚本 `kb_chunk.py` 的切块顺序（见 references/03）：
1. **先按 H2 切**：一个 `##` 标题下讲一件事，作为一级切分单元。
2. **超长再按 H3 切**：某 H2 块超过 `--max-chars`，且内部有 `###`，按 H3 拆。
3. **仍超长按段落智能拼接**：没有 H3 且仍超长，按空行分段，尽量拼到接近 `max-chars` 且不切断句子。
4. **完全没有标题**：整篇当一个条目（并提示你补结构）。
5. **完全没有 H2**：用 H1/H3 兜底切分。

每条自动计算：
- `slug`：标题转稳定 ID，冲突追加 `-2`、`-3`（建议生成后改成语义 slug，见 references/04）。
- `category`：取自文档第一个 H1。
- `tags`：标题路径实词 + 正文高频词（出现≥4 次的两字以上词）+ 你注入的 `--tags`，收敛到 ≤12 个。
- `summary`：首段实质文字前 160 字，含关键动作。
- `fingerprint`：标题 + 正文前 200 字的内容哈希前 12 位，去重依据。

---

## 五、输出格式详解

每条 `.md` 文件结构：
```markdown
---
slug: how-to-refund
title: 如何申请退款
category: 客服
tags: 退款, 流程, 财务
summary: 在「我的订单」提交退款申请，1-3 个工作日原路退回
source: 产品帮助中心.md
fingerprint: a1b2c3d4e5f6
---

# 如何申请退款

（原文档该章节的完整正文）
```
`manifest.json` 结构：
```json
{
  "generated_at": "2026-09-08T...",
  "source": "产品帮助中心.md",
  "count": 12,
  "entries": [
    {"slug": "...", "title": "...", "category": "...", "tags": [...], "summary": "...", "fingerprint": "...", "file": "....md"}
  ]
}
```

---

## 六、真实示例（使用示例）

**示例 1｜用户输入**
> 帮我把这个「产品帮助中心.md」整理成知识库条目，分类按文档结构来，标签用产品、帮助中心、FAQ。

**助手动作**
1. 读 `产品帮助中心.md`，确认有 `#`/`##` 标题。
2. 跑：
   ```bash
   python scripts/kb_chunk.py --input 产品帮助中心.md --out ./entries --title "产品帮助中心" --tags "产品,帮助中心,FAQ" --max-chars 1800
   ```
3. 回报：共切片 12 条，输出至 `./entries`，索引 `manifest.json`。
4. 抽 2 条展示 frontmatter，请用户确认分类/tags 是否合适。
5. 提示：质检按 references/05 清单，确认后导入 ima。

**示例 2｜用户输入**
> 这些研报太长，Agent 一问就瞎编，怎么让它基于真实资料答？

**助手动作**
1. 建议先把研报切片：`--max-chars 1200` 切细。
2. 喂 Agent 时只拼「与问题相关条目」的 `body`，并让 Agent 在回答里引用 `source`。
3. 强调：带出处的条目 = 可溯源，杜绝幻觉（呼应 references/01 的「喂 AI」触发条件）。

**示例 3｜用户输入**
> 我笔记乱七八糟，想搞个第二大脑但不知道从哪开始。

**助手动作**
1. 先问references/01 的「三问」：给谁用、怎么检索、谁来维护。
2. 推荐 PARA 法（项目/领域/资源/归档），帮他把笔记合并切片，category 映射到四类。
3. 提醒：先有 20 条高质量条目跑通闭环，再扩（references/01 误区）。

---

## 七、异常处理与排错

| 现象 | 原因 | 解决 |
|---|---|---|
| 切片只出 1 条 | 文档没用标题层级 | 给文档加 `##` 标题再跑 |
| 一条 5000 字 | 该章节超长且无 H3 | 调小 `--max-chars` 或补 H3 标题 |
| 标签全是乱词 | 正文噪声大 | 用 `--tags` 注入受控词表覆盖 |
| slug 出现 `-2` | 标题重复 | 生成后手动改成语义 slug（references/04） |
| 中文乱码 | 文件非 UTF-8 | 用 UTF-8 另存素材再跑 |
| 想切更碎喂向量 | 默认 1800 偏长 | `--max-chars 1000~1200` |
| 多文件怎么批 | 脚本单文件 | 用 for 循环（见场景 D） |

脚本是纯标准库零依赖，**任何异常都会打印 `[ERROR]`/`[FAIL]` 并以非 0 退出**，不会静默出错。可用 `python scripts/kb_chunk.py --selftest` 随时验证脚本本身完好（exit 0 即通过）。

---

## 八、隐私与合规说明

- 脚本**不联网、不执行外部命令、不删除任何文件**，只读 `--input`、只写 `--out`。
- 含 PII（手机号/身份证/银行信息）的素材：**不要**进公开/共享库，先脱敏（中间四位打码）或留内部（见 references/05 第五节）。
- 他人 copyrighted 内容：仅做个人学习笔记 + 注明出处，勿整本搬运冒充原创（护栏 references/guardrail.md）。
- 经营/财务/报价等敏感数据：入库前确认对外公开范围（护栏软性提醒）。
- 生成内容标注 `source`，保留可追溯性，不伪造出处。

---

## 九、质量门禁（发布前自查，不过不放行）

每条目与整库按 references/05 清单验收：
- 单条：标题能当检索词、能独立答一问、字数 300-3000、有 category、有 2-6 规范标签、summary 含关键动作。
- 整库：分类 ≤3 层且 MECE、标签 ≤30、slug 唯一语义化、最长 ≤3000、抽样检索命中 100%。
- 脚本：`--selftest` 必须 exit 0；真实素材跑通产出 `.md`+`manifest.json`。

---

## 十、进阶技巧

1. **受控词表前置**：先列好标签词再切片，比切完再改标签省 10 倍功夫。
2. **slug 纪律**：定下别改，用语义 slug 不用序号（references/04 第五节）。
3. **相关互指**：重要条目正文末加「参见《xxx》」，形成知识网，检索召回更高。
4. **增量更新**：每次新素材跑切片，比对 `fingerprint` 跳过已有，只验新增（references/05 第四节）。
5. **向量检索重叠**：做 embedding 时相邻条目共享小段，避免边界信息丢失（references/03 第三节）。
6. **多语言分库**：中文/英文别混切，embedding 跨语言会乱。
7. **版本保鲜**：规范类条目 summary 标生效时间，重大变更开新 slug 归档旧条（references/04 第六节）。

---

## 十一、实战演武：一篇真实长文 → 知识条目（端到端演示）

光说方法没感觉，跑一遍。下面是一段「产品帮助中心」节选，演示从素材到条目的全过程。

**输入素材 `帮助中心.md`：**
```markdown
# 产品帮助中心

## 账号与登录

### 如何注册账号
打开官网点击右上角「注册」，使用手机号或邮箱即可注册。注册后需通过验证码完成实名校验。一个手机号只能绑定一个主账号。

### 忘记密码怎么办
在登录页点击「忘记密码」，通过短信验证码重置。若手机号已停用，需联系人工客服提交身份材料解绑。

## 会员与计费

### 会员有哪些档位
会员分为基础版、专业版、旗舰版三档。基础版免费，专业版 39 元/月，旗舰版 99 元/月。年付享受 8 折。

### 怎么开发票
在「我的-账单」页点击「申请发票」，填写抬头与税号后提交，电子发票 1-3 个工作日开具并发送至邮箱。

## 数据安全

### 我的数据安全吗
平台采用传输加密与存储加密，敏感字段脱敏处理。数据所有权归用户，可随时导出或删除。
```

**执行：**
```bash
python scripts/kb_chunk.py --input 帮助中心.md --out ./entries --title "产品帮助中心" --tags "产品,帮助中心,FAQ" --max-chars 1800
```

**输出 `./entries/`（默认按 H2 切块 → 3 条）：**
- `账号与登录.md`：category=产品帮助中心，tags=[账号,登录,产品,帮助中心,FAQ]，summary=注册与找回密码的入口与校验方式
- `会员与计费.md`：tags=[会员,计费,产品,帮助中心,FAQ]，summary=三档会员资费与开票路径
- `数据安全.md`：tags=[安全,数据,产品,帮助中心,FAQ]，summary=传输与存储加密及数据归属

**关键洞察：** 因为本例 H2 下各段都不超 `--max-chars 1800`，脚本按 H2 切成 3 条粗粒度。若你想要更细的「如何注册账号 / 忘记密码 / 会员档位 / 开发票 / 数据安全」5 条，有两个办法：
1. 调小 `--max-chars 600`，让 H2 块因超长而再按 H3 切；
2. 直接把素材的 H2 改成 H3（即把「账号与登录」降一级），切块粒度立即变细。

**结论：** 切块粒度 = `--max-chars` × 素材标题层级，你两手都能调。

---

## 十二、切片参数速查表

| 你的目标 | --max-chars | 标题层级建议 | 结果 |
|---|---|---|---|
| 人读知识库条目 | 1500-2000 | 保留 H2 | 每主题一条，约 800-1800 字 |
| 向量检索 embedding | 1000-1300 | 可降一级 | 语义完整、不稀释 |
| 喂 Agent 上下文 | 800-1200 | 降一级 | 单原子知识点，省 token |
| 长文归档保留结构 | 2500-3000 | 保留 H2/H3 | 大章节不碎 |
| 极细 FAQ 库 | 400-600 | H3 当主切 | 一问一答一条 |

规律：max-chars 越小、标题层级越深 → 条目越碎越多；反之越粗越少。先用 1800 跑一版看效果，再调。

---

## 十三、建库反模式三则（真实踩坑）

**反模式 1：贪大求全，一上来建「公司全部知识」**
- 现象：建库第一天就想把 500 篇文档全切了，结果分类树没想清楚，切出来 500 条垃圾。
- 正确：从一个高频痛点切（如客服 FAQ 20 条），跑通「切→质检→入库→检索」闭环，再扩。

**反模式 2：标签失控，一条 15 个标签**
- 现象：脚本从正文高频词抓了一堆标签，看起来很全，检索时反而散。
- 正确：先定 ≤30 个受控词表，用 `--tags` 注入；脚本的高频词只作补充，最终人工收敛到 2-6 个。

**反模式 3：巨无霸条目，一条 6000 字**
- 现象：某个 H2 章节特别长又没 H3，切出来一条顶十行。
- 正确：给素材补 H3 标题，或调小 max-chars；检索时巨无霸会被「平均掉」，相关段落召回率低。

---

## 十四、团队知识库治理与长期保鲜

建完不是结束，腐坏是常态。治理要点：
1. **责任到人**：每条 `source` 可追溯，配一个维护 owner，避免「大家都管 = 没人管」。
2. **定期巡检**：每月用 references/05 清单抽 20 条验收，重点查过期与重复。
3. **过期机制**：规范类条目 summary 标生效时间（如「2026-09 生效」）；重大变更开新 slug，旧条目标「已归档，见《xxx》」，别直接覆盖。
4. **权限分层**：公开库 / 内部库 / 机密库分开；含 PII 的条目脱敏或留内部，不进公开库。
5. **勿物理删**：归档不删除，保留追溯；用 `fingerprint` 识别重复。
6. **增量更新**：新素材跑切片，比对旧 `manifest.json` 的 fingerprint，跳过已有，只验新增（references/05 第四节）。

---

## 十五、与 ima 知识库 / 向量库 / Agent 对接实操

本 Skill 的产物是「标准 .md + manifest.json」，对接三方都很顺：

- **进 ima 知识库**：把 `.md` 条目直接上传，frontmatter 的 `tags` 当标签、`summary` 当简介、`body` 当正文。ima 会自动索引，用户自然语言提问即可召回。
- **进向量库**：用每条 `body`（完整正文）做 embedding，不要只嵌 `summary`（语义会丢）。检索时返回 `title + summary` 做卡片，点开才看 `body`。
- **当 Agent 上下文**：把「与问题相关条目」的 `body` 拼进 prompt，并让 Agent 在回答里引用 `source` 字段——带出处的回答可溯源，杜绝幻觉。
- **多库策略**：按业务域（账号/计费/安全）或语言分库，别混切混喂，embedding 跨域跨语言会乱。
- **权限提醒**：对外共享的库不要放 PII；内部库可放脱敏后的经营数据。

---

## 十六、参考资源索引（本包内）

- references/01_知识管理体系.md —— 何时建库、Zettelkasten/PARA/第二大脑取舍
- references/02_信息架构与分类法.md —— MECE 分类、受控标签、命名规范
- references/03_分块策略与粒度.md —— 切块大小、切分锚点、特殊内容处理
- references/04_条目模板与元数据.md —— frontmatter 字段字典、正文四型模板
- references/05_质量检查清单.md —— 单条/整库验收、病害对照、增量流程
- scripts/kb_chunk.py —— 切片与结构化脚本（纯标准库，含 --selftest）
- hooks/guardrail.md —— 合规护栏，硬拒违规请求

---

## 十七、常见问题快答（FAQ）

**Q1：我的文档没有标题层级，只有一大段文字，能切吗？**
能。脚本检测不到标题时，整篇当一个条目并提示你补结构。建议先用 `#`/`##` 把文档分章节，切出来才有分类价值。

**Q2：切出来的条数太少/太多怎么办？**
调 `--max-chars`：调小 → 更碎更多条；调大 → 更粗更少条。同时检查素材标题层级是否够深（H3 能进一步切）。

**Q3：标签抓了一堆没用的词怎么处理？**
用 `--tags` 注入你的受控词表（如 `--tags "产品,帮助中心"`），脚本会优先用你给的词，正文高频词只作补充，最终人工收敛到 2-6 个。

**Q4：slug 出现 `-2`、`-3` 能改吗？**
能也建议改。生成后把重复 slug 手动改成语义 slug（如 `refund-user`），slug 定下别再改，否则历史链接/检索断裂（见 references/04 第五节）。

**Q5：怎么避免切出来的条目 Duplicate（重复）？**
脚本每条算 `fingerprint`（内容哈希）。增量更新时比对旧 `manifest.json` 的 fingerprint，相同的跳过，只处理新增（见 references/05 第四节）。

**Q6：能直接喂给 AI 防止它瞎编吗？**
能。把相关条目的 `body` 拼进 prompt，并让 AI 回答时引用 `source`。带出处的回答可溯源，大幅降低幻觉（见第十五节）。

**Q7：含客户手机号/身份证的素材能建库吗？**
公开/共享库不能放，先脱敏（中间四位打码）或留内部库。护栏也会在涉及 PII 时提醒（见第八章、guardrail.md）。

---

## 十八、一句话总结

建知识库不是囤资料，是把资料切成「能检索、能溯源、能喂 AI」的结构化条目。本 Skill 给你切片工具 + 一套经过验证的方法论 + 入库质检门禁，让你从「攒了一堆找不到」变成「问一句就出答案」。
